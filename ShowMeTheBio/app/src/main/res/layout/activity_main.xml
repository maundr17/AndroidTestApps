<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity"
    android:background="#ffff6b7d">

    <ImageView
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:id="@+id/bachImageView"
        android:layout_marginTop="35dp"
        android:src="@drawable/bach_profile"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <ImageView
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:id="@+id/mozartImageView"
        android:src="@drawable/mozart_profile"
        android:padding="0dp"
        android:layout_below="@+id/bachText"
        android:layout_alignLeft="@+id/bachImageView"
        android:layout_alignStart="@+id/bachImageView"
        android:layout_marginTop="51dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:text="Bach"
        android:id="@+id/bachText"
        android:layout_below="@+id/bachImageView"
        android:layout_centerHorizontal="true" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:text="Mozart"
        android:id="@+id/mozartText"
        android:layout_below="@+id/mozartImageView"
        android:layout_centerHorizontal="true" />
</RelativeLayout>
